{
  "name": "lms",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm run dev",
    "dev": "cd frontend && npm run dev"
  }
}
